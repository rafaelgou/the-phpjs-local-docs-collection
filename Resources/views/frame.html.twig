{% extends 'layout.html.twig' %}
{% block nav %}
                <ul class="nav navbar-nav">
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Main Menu <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        {% for sectionId,section in sections %}
                            <li><a href="{{ base_url }}/#section{{ sectionId }}">{{ section.label }}</a></li>
                        {% endfor %}
                    </ul>
                  </li>
                </ul>
{% endblock nav %}
{% block content %}
<iframe name="inner" style="width:100%;" height="auto" src="{{ docurl }}"></iframe>
{% endblock content %}
{% block footer %}{% endblock footer %}

